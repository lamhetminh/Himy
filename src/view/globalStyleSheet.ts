import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  globalButton: {
    display: 'flex',
    textAlign: 'center',
    paddingLeft: 30,
    paddingRight: 30,
  },
});
